# PRD Coverage Good Fixture

The app has a public visitor flow at `/sources` where visitors can search the
source library, and an operator console at `/dashboard` where the operator can
approve generated episodes after review.

