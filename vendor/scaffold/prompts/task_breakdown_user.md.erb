Break down the following specification into tasks:

<%= spec_content %>

---

CRITICAL REMINDER: Your response must be ONLY a JSON array. Do not include any
prose, markdown, tables, summaries, or commentary. Start your response with `[`
and end with `]`. No text before or after the JSON.
